# AWS Events API Terraform module

Terraform module which creates the resources to host a events api on AWS.

## Usage

```hcl
module "events" {
  source      = "genstackio/layer-api-events/aws"
  
  # ...
}
```
